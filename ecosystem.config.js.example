// PM2 Ecosystem Configuration
// Copy this file to ecosystem.config.js and adjust paths

module.exports = {
  apps: [{
    name: 'pi-gigs',
    script: 'node_modules/next/dist/bin/next',
    args: 'start',
    cwd: '/var/www/pi-gigs',  // Change to your project path
    instances: 2,  // Number of instances (or 'max' to use all CPU cores)
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: './logs/pm2-error.log',
    out_file: './logs/pm2-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '1G',
    watch: false,
    ignore_watch: ['node_modules', '.next', 'logs'],
    
    // Advanced options
    min_uptime: '10s',
    max_restarts: 10,
    restart_delay: 4000,
    
    // Environment variables (if not using .env file)
    // env_production: {
    //   NODE_ENV: 'production',
    //   PORT: 3000
    // }
  }]
};
